# Netlify – deploy do app (Vite SPA)
# Não altera a Vercel; você pode usar os dois.

[build]
  command = "npm run build"
  publish = "dist"

# SPA: todas as rotas caem no index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
